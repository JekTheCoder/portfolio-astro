---
interface Props {
  href: string;
}

const { href } = Astro.props;
---

<a
	href={href}
  class="after:bg-bold text-bold after:h-px after:block after:w-0 hover:after:w-full after:transition-all"
>
	<slot />
</a>
